<header>
  <nav>
    <ul>
      <li class='about <%= "active" if is_active?(params[:action], 'about') %>'>
        <%= link_to "About", root_path %>
      </li>
      <li class='blog <%= "active" if is_active?(params[:action], 'blog') %>'>
        <%= link_to "Blog", posts_path %>
      </li>
      <li class='portfolio <%= "active" if is_active?(params[:action], 'portfolio') %>'>
        <%= link_to "Portfolio", portfolio_path %>
      </li>
      <li class='contact <%= "active" if is_active?(params[:action], 'contact') %>'>
        <%= link_to "Contact", contact_path %>
      </li>
    </ul>
  </nav>
</header>

